Upstream-Status: Inappropriate (at the moment) as Cheri specific

diff --git a/jitterentropy-base-user.h b/jitterentropy-base-user.h
index b7d21e8..3c737c8 100644
--- a/jitterentropy-base-user.h
+++ b/jitterentropy-base-user.h
@@ -270,8 +270,12 @@ static inline void jent_memset_secure(void *s, size_t n)
 	OPENSSL_cleanse(s, n);
 #else
 	memset(s, 0, n);
+#ifdef __CHERI_PURE_CAPABILITY__
+	__asm__ __volatile__("" : : "C" (s) : "memory");
+#else
 	__asm__ __volatile__("" : : "r" (s) : "memory");
 #endif
+#endif
 }
 
 static inline long jent_ncpu(void)
