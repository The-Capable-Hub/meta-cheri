Upstream-Status: Inappropriate [CHERI specific]
Index: meson-1.3.1/mesonbuild/modules/gnome.py
===================================================================
--- meson-1.3.1.orig/mesonbuild/modules/gnome.py
+++ meson-1.3.1/mesonbuild/modules/gnome.py
@@ -1881,7 +1881,7 @@ class GnomeModule(ExtensionModule):
             GType
             {func_prefix}@enum_name@_get_type (void)
             {{
-            static gsize gtype_id = 0;
+            static GType gtype_ret = 0;
             static const G@Type@Value values[] = {{'''))
 
         c_cmd.extend(['--vprod', '    { C_@TYPE@(@VALUENAME@), "@VALUENAME@", "@valuenick@" },'])
@@ -1890,11 +1890,11 @@ class GnomeModule(ExtensionModule):
         c_cmd.append(textwrap.dedent(
             '''    { 0, NULL, NULL }
             };
-            if (g_once_init_enter (&gtype_id)) {
+            if (g_once_init_enter_pointer (&gtype_ret)) {
                 GType new_type = g_@type@_register_static (g_intern_static_string ("@EnumName@"), values);
-                g_once_init_leave (&gtype_id, new_type);
+                g_once_init_leave_pointer (&gtype_ret, new_type);
             }
-            return (GType) gtype_id;
+            return gtype_ret;
             }'''))
         c_cmd.append('@INPUT@')
 
