This is an addition made to the cheri repository, it needs a corrisponding
change to clang, which we don't have currently.

Upstream-Status: Inappropriate as upstream is us

diff --git a/configure b/configure
index 5c64e1e5..60b73f73 100755
--- a/configure
+++ b/configure
@@ -671,8 +671,8 @@ fi
 if test "$ARCH" = "riscv64" ; then
 trycppif __riscv_float_abi_soft "$t" && SUBARCH=${SUBARCH}-sf
 trycppif __riscv_float_abi_single "$t" && SUBARCH=${SUBARCH}-sp
-trycppif __CHERI_PURE_CAPABILITY__ "$t" && SUBARCH=${SUBARCH}cheri
-trycppif __clang__ "$t" && SUBARCH=${SUBARCH}-clang
+# trycppif __CHERI_PURE_CAPABILITY__ "$t" && SUBARCH=${SUBARCH}cheri
+# trycppif __clang__ "$t" && SUBARCH=${SUBARCH}-clang
 fi
 
 if test "$ARCH" = "sh" ; then
