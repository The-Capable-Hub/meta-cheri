Update the template used for generating _get_type() functions so that they use
g_once_init_{enter,leave}_pointer where appropriate.

See the upstream issue:
  https://gitlab.gnome.org/GNOME/glib/-/issues/2842
for more information.

Upstream-Status: Inappropriate [CHERI specific]

--- a/atk/atk-enum-types.c.template	2024-01-06 01:16:15.000000000 +0000
+++ b/atk/atk-enum-types.c.template	2025-02-03 19:19:35.564589978 +0000
@@ -15,9 +15,9 @@
 GType
 @enum_name@_get_type (void)
 {
-  static gsize g_define_type_id__volatile;
+  static GType g_define_type_id__volatile;
 
-  if (g_once_init_enter (&g_define_type_id__volatile))
+  if (g_once_init_enter_pointer (&g_define_type_id__volatile))
     {
       static const G@Type@Value values[] = {
 /*** END value-header ***/
@@ -31,7 +31,7 @@
       };
       GType g_define_type_id =
         g_@type@_register_static (g_intern_static_string ("@EnumName@"), values);
-      g_once_init_leave (&g_define_type_id__volatile, g_define_type_id);
+      g_once_init_leave_pointer (&g_define_type_id__volatile, g_define_type_id);
     }
   return g_define_type_id__volatile;
 }
